@model Core.Entities.Movie

@{
    ViewData["Title"] = "Moyenne des notes";
    var averageRating = ViewBag.AverageRating;
}

<h2>@ViewData["Title"]</h2>

@if (Model == null)
{
    <p>Film introuvable.</p>
}
else
{
    <h3>@Model.Name</h3>
    <p>Average rating: @averageRating</p>
}